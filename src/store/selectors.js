export const selectBicycles = state => state.bicycles.items;

export const selectStats = state => state.bicycles.stats;

export const selectIsLoading = state => state.bicycles.isLoading;

export const selectError = state => state.bicycles.error;
